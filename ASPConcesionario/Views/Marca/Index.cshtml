@model IEnumerable<ASPConcesionario.Models.Parametros.ModeloMarca>

@{
    ViewBag.Title = "Index";
}

<h2>Marcas</h2>

<p>
    @Html.ActionLink("Nuevo Registro Marcas", "Create",new { }, new { @class = "btn btn-success" })
</p>
<input type="text" name="txtFiltro" id="txtFiltro" />
<button type="submit" class="btn btn-info " > Filtrar</button>
<table class=" table table-bordered ">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>Acciones</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.Id }, new { @class="btn btn-primary"})
            @Html.ActionLink("Detalles", "Details", new { id=item.Id }, new { @class = "btn btn-warning" }) 
            @Html.ActionLink("Eliminar", "Delete", new { id=item.Id }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
@Html.Partial("_Footer_pageList")
